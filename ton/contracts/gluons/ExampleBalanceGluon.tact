// Example Balance Gluon (TON scaffold)

import "./IGluon.tact";

contract ExampleBalanceGluon implements IGluon {
  fun call(data: slice) {
    // decode data and compute balance updates if needed
  }
}
